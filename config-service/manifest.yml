---
applications:
- name: config-service
  memory: 512M
  instances: 1
  host: config-service-${random-word}
  domain: cfapps.io
  path: target/config-service.jar
  env:
    SPRING_PROFILES_DEFAULT: cloud
    DEBUG: "true"
    debug: "true"
